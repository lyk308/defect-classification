# 项目V1.0：收尾 + 简历项目描述 —— 第6天任务单（周六综合练习）

**日期：2026-08-22 周六（第42天）**
**时长：2 小时（16:30–18:30）**，建议节奏：收尾 30min → 简历 70min → 阶段回顾 20min

---

## 今日目标

1. **V1.0 收尾**：文件完整性检查 + 生成 `requirements.txt`（工程规范加分项）
2. **简历项目描述**：把 V1.0 浓缩成 2–3 行项目经历，按岗位版本微调（今天的重头戏）
3. **阶段回顾**：阶段二收官，学习总纲已更新（我更新好了，你确认）

---

## 一、V1.0 收尾清单（30 分钟）

### 1. 文件完整性检查（5 分钟）

在 JupyterLab 文件树里确认这些都在 `项目V1.0` 下：

- [ ] `README.md`（昨天写的）
- [ ] `images/` 5 张图（class_distribution / confusion_matrix / roc_curve / feature_importance / coef_heatmap）
- [ ] `defect_data.csv`
- [ ] 笔记本 1–5（启动 / 数据清洗 / 多模型对比 / 调参 / 评估）

缺哪个补哪个——**收尾 = 别人拿到文件夹能完整复现**。

### 2. 生成 requirements.txt（15 分钟）

**requirements.txt 是什么**：别人拿到你的项目，想复现环境，只需要一条 `pip install -r requirements.txt`。这是工程规范里最基本的仪式感，面试官看到会加分。

在 `项目V1.0` 下新建文本文件 `requirements.txt`，**手写核心依赖**（推荐，干净）：

```text
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
jupyterlab>=4.0
```

**为什么手写而不是 `pip freeze > requirements.txt`？**

| 方式 | 结果 | 问题 |
|------|------|------|
| `pip freeze` | 把你电脑上**所有**包（几百个）全列出来 | 别人装了可能冲突；可读性差 |
| 手写核心依赖 | 只列项目真正用到的 6 个 | 干净、兼容性好 ✅ |

**讲解**：
- `pandas>=2.0` 意思是"装 2.0 及以上版本"，`>=` 给版本留余地（你用的是 3.0，别人装 2.x 也能跑）。
- 不写具体版本号（如 `pandas==2.2.3`）是因为项目对版本不敏感，放宽限制别人更容易装上。

验证：在新环境里 `pip install -r requirements.txt` 能一键装齐（不用真验证，知道这个用法即可）。

### 3. 项目结构整理（选做，为 GitHub 做准备）

建议将来传 GitHub 时用这个结构（现在**不用动文件**，上传前整理即可）：

```
defect-classification/
├── README.md
├── requirements.txt
├── defect_data.csv
├── images/
└── notebooks/          ← 把 1-5 号笔记本放进来
```

---

## 二、简历项目描述（70 分钟，今天的重头戏）

### 1. 简历项目的"3行公式"

| 行 | 写什么 | 例子 |
|----|--------|------|
| 第1行 | 项目名 + 时间 + 一句话说清"做什么" | 政务系统缺陷严重度智能分类（2026.08）：基于运维缺陷场景的 4 分类模型 |
| 第2行 | 技术动作（怎么做的） | 特征工程 + 5 模型交叉验证对比 + GridSearchCV 调参 |
| 第3行 | 量化结果 + 业务价值 | 轻微类召回率 0.17→1.00，特征重要性与业务规则一致（可解释） |

**每个项目 3 行、每行 1 个关键点，面试官 20 秒扫完。**

### 2. 数字怎么选（诚实原则，今天最重要的判断）

你的项目有几个数字，**不是都能往简历上放**：

| 数字 | 建议 | 原因 |
|------|------|------|
| 轻微类召回率 0.17 → 1.00 | ✅ **必写** | 调参前后的对比故事，体现你的问题意识和调参能力 |
| 5 种模型交叉验证对比，逻辑回归最优 0.954 | ✅ 写 | 体现"先对比再选型"的科学流程 |
| 特征重要性与业务规则一致（可解释） | ✅ 写 | AIOps 面试最加分的一句 |
| 测试集准确率 100% | ⚠️ **要带限定或干脆不写** | 面试官第一反应是"真实数据不可能 100%"；要么写"500 条模拟数据上准确率 100%"，要么不写，用召回率故事代替 |

**原则：简历上的每个数字，都要经得起"这数字怎么来的？"这一问。**

### 3. 按岗位版本微调（重点表）

你已有 6 个简历版本，同一个项目，**侧重点不同**：

| 简历版本 | 侧重点 | 描述示例（可直接改到自己简历） |
|---------|--------|------------------------------|
| **智能IT运维版**（投递优先级最高） | 缺陷分级 + 可解释 + 高危零漏报 | 见下方【示例A】 |
| **SRE版** | 数据驱动、可观测性、上线前审查 | 见下方【示例B】 |
| **数据分析师版** | 分析全流程 + 可视化报告 | 见下方【示例C】 |
| **运维开发版** | Python 工程能力 + 可复现 | 见下方【示例D】 |
| DBA版 | 与本项目关系弱，可不写或简化 | 只留技术栈一行 |

**示例A（智能IT运维版）**：
```
政务系统缺陷严重度智能分类（Python/scikit-learn，2026.08）
• 基于人社局政务系统驻场运维的缺陷记录场景，独立完成500条缺陷数据（致命/严重/一般/轻微）多分类建模全流程
• 完成特征工程（OneHot编码、分层抽样、标准化）与5种模型交叉验证对比，逻辑回归最优（0.954）；GridSearchCV调参将轻微类召回率由0.17提升至1.00，高危缺陷（致命/严重）零漏报
• 输出混淆矩阵、多分类ROC-AUC、特征重要性完整评估，特征重要性与业务打分规则一致，模型可解释
```

**示例B（SRE版）**：
```
缺陷严重度智能分类模型（2026.08）
• 面向运维缺陷分级场景构建4分类模型，将人工分级规则转化为可复用的数据模型
• 交叉验证+GridSearchCV完成模型选型与调参（逻辑回归，交叉验证准确率0.954），轻微类召回率0.17→1.00
• 输出完整评估报告（混淆矩阵/ROC-AUC/特征重要性），支持模型上线前的可解释性审查
```

**示例C（数据分析师版）**：
```
政务系统缺陷数据分析与分类建模（2026.08）
• 清洗并分析500条缺陷记录（类别分布、类不平衡诊断），完成OneHot特征工程
• 对比5种分类模型，逻辑回归最优（交叉验证0.954）；调参后测试集准确率100%（模拟数据）
• 产出含混淆矩阵、ROC曲线、特征重要性的可视化分析报告，验证特征重要性与业务规则一致性
```

**示例D（运维开发版）**：
```
缺陷分类模型与工程化（Python，2026.08）
• 使用Pandas/sklearn实现缺陷严重度多分类建模全流程，代码含详细注释
• 输出 requirements.txt、README、编号化Jupyter Notebook，项目可一键复现
• 评估完整（交叉验证/GridSearchCV调参/ROC-AUC/混淆矩阵），轻微类召回率0.17→1.00
```

### 4. 面试自我保护（每个数字都要能解释）

写完简历，先自问这三问：

**Q1：面试官问"准确率为什么 100%？"**
答：数据是**模拟生成**的，严重程度由特征按固定规则打分得出，特征和标签完全对应、数据完全可分，所以模型能学全规则。**真实数据会有噪声，准确率会降，届时靠交叉验证选 C 防过拟合**。→ 这个回答既诚实又展示你懂原理。

**Q2：面试官问"这项目对运维有什么价值？"**
答：把人工分级经验**规则化、自动化**，且特征重要性和业务规则一致，**模型可解释，运维才敢用**。高危缺陷零漏报，符合"宁可多报、不可漏报"。

**Q3：面试官问"给你真实数据你会怎么做？"**
答：先做数据校验和清洗，重点补轻微类样本（真实数据更不平衡）；加文本特征（缺陷描述）；上线后监控指标漂移。→ 这句正好衔接你的阶段三计划（LSTM/BERT/RAG），说明你已经在规划。

---

## 三、阶段回顾（20 分钟）

### 阶段二自检（全部 ✅ 就收官了）

- [ ] 数据清洗与特征工程（OneHot/分层抽样/标准化/防数据泄露）
- [ ] 5 种模型交叉验证对比（逻辑回归 0.954 最优）
- [ ] GridSearchCV 调参（C=10 解决轻微类召回率 0.17→1.00）
- [ ] 完整评估（混淆矩阵/多分类ROC-AUC/特征重要性/类别分布）
- [ ] README.md + 5 张配图
- [ ] requirements.txt
- [ ] 简历项目描述（4 个岗位版本）

### 学习总纲更新（我已更新，你确认）

- 阶段一、阶段二 → ✅ 已完成（2026-08-22）
- 技能清单新增"项目实战"行
- 9 月：简历投递试水（国企运维岗/数据分析师）+ 巩固；10 月：阶段三（LSTM→BERT→LLM→RAG）

---

## 四、今日验收标准

- [ ] 1. `requirements.txt` 创建完成，内容 6 个核心依赖
- [ ] 2. 简历项目描述按"3行公式"写完，数字经得起追问
- [ ] 3. 至少完成"智能IT运维版"的简历描述
- [ ] 4. 能口头回答"为什么 100%"三连问

---

## 五、今日笔记模板（跑完填空）

```markdown
## 2026-08-22 周六（第42天）项目V1.0：收尾 + 简历

### 完成内容
- requirements.txt：____
- 简历项目描述：完成____个版本（____）

### 简历最终版（粘贴你写好的版本）
（智能IT运维版全文）

### 三连问自答
- 为什么100%：____
- 项目价值：____
- 真实数据怎么做：____

### 阶段二总结
- 最大的收获：____
- 最想加强的：____
```

---

## 六、参考答案

- requirements.txt 参考：6 个核心依赖（见本任务单第一节，直接抄即可）
- 简历描述参考：示例A–D（见第二节，**按你的实际数字微调后使用**）
- 总纲更新：`AI学习总纲.md`（阶段一/二 ✅，9月投递试水计划已写入）

---

**阶段二正式收官！🎉 下一个里程碑：10 月阶段三——LSTM 时序异常检测 → BERT 日志分类 → LLM 日志摘要 → RAG 知识库问答（项目 V2.0）。9 月是投递试水 + 巩固期，我们按节奏走。**
